services:
  searxng:
    image: searxng/searxng:latest
    ports:
      - "8080:8080"
    volumes:
      - ../../tools/searxng:/etc/searxng
    environment:
      - SEARXNG_BASE_URL=http://localhost:8080
